stages:
  - build

build:
  stage: build
  image: cje172/ride-or-not:hanium
  script:
    - chmod +x ./gradlew
    - ./gradlew assembleDebug
  artifacts:
    paths:
      - app/build/outputs/

cache:
  paths:
    - /cache